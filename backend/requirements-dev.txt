# TEST TOOLING ONLY — never installed in production.
# Install alongside requirements-prod.txt:
#   pip install -r requirements-prod.txt -r requirements-dev.txt
#
# Pinned so a CI failure is always a code change, never a silent tool upgrade.
# Nothing here may pull a different numpy/pandas/smartmoneyconcepts than
# requirements-prod.txt — CI asserts that explicitly before running the suite.

pytest==9.1.1
pytest-asyncio==1.4.0
aiosqlite==0.22.1
fakeredis[lua]==2.37.0
anyio==4.14.2
