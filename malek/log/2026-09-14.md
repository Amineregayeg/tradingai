# Malek — log, 2026-09-14

## 00:25 WAT — first recap: where the project stands

**Background (before this log existed).** TradingAI is a FastAPI + React platform that trades Salim's strategy
rules on crypto (BTC, ETH). Earlier phases built the rule engine, a simulator, a decision record for every signal,
and a deployment. The plan to trade through MetaTrader 5 was dropped on 2026-09-10 in favour of an Alpaca paper
account, long only. Parts A–D of that plan are built; the engine has been held since 2026-09-01 while the order path
is made safe.

**2026-09-12**
- Deploy "D" to production: commit `6ae6aca`, migrations 0012 and 0013, verified by content.

**2026-09-13** — the order path, made to tell the truth about what the venue did. All passed review unless noted.
- `B433` — an unreadable fill price is now "unknown", never a crash or an invented number.
- `B427` + `B438` + `B439` (`a451ec1`, then fixed at `fb3dab6`) — an order is re-checked until it settles
  (bounded by a real time limit) before it is recorded. A close counts only once it actually fills. The kill switch
  now closes every position instead of stopping after the first.
- `B440` + `B441` (`fb3dab6`) — a failed order send that may still have created an order is looked up by its id
  instead of being recorded as refused (and the SDK no longer re-sends orders on a gateway timeout). Every Alpaca
  network call now has a timeout.
- Filed, not yet fixed: `B442` (kill switch vs an entry under way; now being built), `B443` (kill switch vs the web
  proxy's 120-second limit), `B444` (Alpaca positions are not yet managed or recorded).
- The first-order runbook was rewritten for the new builds, including a check that tells a build that failed
  review apart from the one that passed.

**Learned**
- A test of a time limit needs a slow fake venue; a fast one hides the bug (it hid `B427`'s).
- `requests` raises the same error type for "never sent" and "sent, then the connection dropped"; only the error's
  underlying cause tells them apart.

## 00:45 WAT
- Handoff set up: this branch, the root README on `main` pointing here (`89d4cfe`), and a 2-hour update loop.
- Filed `B445` and `B446` (`9e96359`), found by the execute seat: the kill switch's report can drop rows, because the
  broker manager flattens each adapter's error, and a cancelled kill switch returns as if it finished. Ruled: finish
  closing, record every row, then report the cancellation. Queued right after `B442`.

## 01:15 WAT
- Deployed `fb3dab6` to production after testing migrations 0014–0015 on a scratch copy of the database.
- Ran the first real orders on the Alpaca paper account (three rounds, ending flat). Filed `B447`–`B451`: the order path
  cannot trade Alpaca crypto as built (no attached stops, symbol spelling mismatch, a missing price, fees in kind, a $10
  minimum). The order-confirmation fix (`B427`) works on the real venue.
- Wrote the `B428b` brief (`T-0144`) from those measurements.

## 02:50 WAT
- `B442` passed review (`87466b6` records it). `B445` + `B446` landed as `32a7610`, and follow-up (2c) as `4a74eab`; both
  are in review.
- An unexplained test failure was named: a test that raced the code it tested, under CPU load (`B452`). It was a test
  flaw, not a product defect, and is fixed in `4a74eab`.
- Migration 0016 passed the scratch-copy test. The deploy waits on the two reviews.
- Set up a 15-minute check that the working sessions are never idle for no reason. Background test runs now wake
  their session when they finish.
- Learned: a cancellation test needs a fake venue that pauses, or it proves nothing.

## 04:50 WAT
- `B445`/`B446` (`32a7610`) and follow-up (2c) (`4a74eab`) passed review.
- (2d) landed as `ab64c03`: the kill switch's "already in progress" answer now hides secrets at any depth, not just at
  the top level. It is in review.
- Found that seven more kill-switch tests shared `B452`'s timing race. Review re-measured each check they carried (10
  clean passes, 10 failures with the bug planted) and all held. Test-only fixes (2e) are being built.
- The kill-switch release is `ab64c03`, and it deploys after (2d)'s review and a pinned full test run.
- Learned: a test that holds an operation open must release that hold on its own after a time limit, or a protected
  (shielded) operation makes the test hang instead of fail.

## 07:50 WAT
- Deployed the kill-switch release `ab64c03` (database 0016) after its migration test and a full test run pinned to that
  commit: item 3 is done.
- Found `B453` in the deployed code (a cancelled kill switch can lose its audit record; latent). The fix (2f) is being
  built and measured.
- (2e) landed (`45161a6`): test-only, making kill-switch tests deterministic under CPU load.
- Reviewed and reworked the `B428b` design three times. Ran probe rounds 4 and 5 on the paper account:
  - Alpaca's paper quotes are often stale
  - no listing delay after fills
  - closes sent as normal sell orders with our own ids work, even below $10
  - the fee is 0.25% on both sides
  - a refused order's id can be reused
  Filed `B454`–`B456`.
- One decision was put to Malek: what the engine may do on its own after a restart.
- Learned: a design brief written from probe data still needs an independent attack. Three rounds found a deploy that
  would liquidate the account, a restart that would abandon live positions, and a shared position id.

## 08:50 WAT
- The `B453` fix (2f) landed as `c1589e2`: a cancelled kill switch now always leaves its audit record and reports the
  cancellation, on all three paths. Waiting for review.
- `B437`'s full test record run started (about 5 hours). Review is finishing (2e).
- No change on Amine's side.

## 12:55 WAT
- Sped up verification after Malek said it was too slow: the machine had 16 cores and the test runs used one. Runs are
  now parallel, re-runs are limited to files a change touches, and there are no duplicate re-runs.
- `B437`'s own checks passed (42/42), including three tests rebuilt to catch their bugs directly. The `B428b` design was
  approved, and building its first commit started.
- Probe round 6 on the paper account: cancelling a resting sell frees the position immediately.
- Filed `B457` (quantities sent in scientific notation), `B458` (6-decimal quantity columns), `B459` (a capability scan
  gap), `B460` (settle writes not awaited at stop), `B461` (reconciler spelling mismatch).
- Both working sessions stopped for about 2 hours on an API certificate or connection error. They were resumed, and the
  seat watch now detects that state.
- Learned: a watch that reads a network filesystem can hang exactly when you need it; bound every scan.

## 13:00 WAT
- The `B453` fix (2f, `c1589e2`) passed review; it deploys with `B437`. Nothing else changed since 12:55.

## 13:58 WAT
- DEPLOYED `e7d7c81` (`B437` + the `B453` fix), checked by content. Database unchanged at 0016.
- `B428b` commit (i) is built, and its record run is going. Migration 0017 passed on a Postgres scratch copy.
- Filed `B462` and `B463` (B437 follow-ups). Amended `B432` (the network in tests) and `B457` (the venue accepts exponent-form quantities, measured).

## 14:50 WAT
- **Registered in advance:** review registered the mutation checks for `B428b` commits (ii), (iii) and (iv), and for
  `B437b` and `T-0145` (`B430`).
- **Design rulings:** `T-0144/PLAN.md` revisions 6–7 and `T-0145/PLAN.md` revisions 2–3.
- **Register:**
  - `B430` amended in `9bfddb2`: the one-token switch would take the API down at boot.
  - `B464` filed in `1086104`.
  - Earlier in the hour: `56a730f` (`B437` recorded as deployed) and `514ccb6` (`B457`).
- **Probe round 7** (paper, ended flat): a reused order id is refused (`422`, "client_order_id must be unique"). 36- and
  40-character ids work. Fill-history pagination is complete, and `after` is exclusive.
- **Commit (i):** the record run was relaunched twice, first for a script fault and then for an arm that missed its
  registered mutant (`T-5`). It is still running.
- **Learned:** production has no Alpaca credentials in its environment. Only the saved broker connection holds them.
